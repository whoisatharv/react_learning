const fs=require("fs")
var thisModule=this;
exports.processRequest=function(request,response)
{

//1) extract roll number and name from the request object
//2)// check if the roll number exists.
 //if yes then send back the same form , but the textboxes should contain the same data that user had feeeded,buy with respective error message
//3) add record to data file
//4) send back the same studentaddform.html , but this time with a  that the stdent has been added

}